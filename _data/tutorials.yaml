- name: gridfile
  steps:
    - Introduction, intro
    - Overview, overview
    - Define metadata, define
    - Create new gridfile, new
    - gridfile objects, object
    - Add data sources, add
    - Load data, load

- name: stateVector
  steps:
    - Concepts, concepts
    - Dimension Indices, dimension-indices
    - Create State Vector, new
    - Add Variables, add
    - Design, design
    - Sequences, sequence
    - Means, mean
    - Weighted Means, weighted-mean
    - Build Ensemble, build

- name: ensemble
  steps:
    - Introduction, intro
    - Save .ens files, save
    - Ensemble objects, object
    - Load ensemble, load
    - Load data subsets, subset
    - Summarize loaded data, info
    - Add ensemble members, add

- name: ensembleMetadata

- name: PSM
  steps:
    - PSM Objects, object
    - Estimate Observations, estimate
    - Run PSM Directly, run
    - Linear PSMs, linear

- name: kalmanFilter

- name: particleFilter

- name: optimalSensor
